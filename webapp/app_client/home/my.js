angular.module('meanApp', [])

// inject the server_operations service factory into our controller
.controller('myCtrl', ['$scope', '$uibModal', '$http','server_operations', '$log', '$window', '$rootScope',  '$filter', function($scope, $uibModal, $http, server_operations, $log, $window, $rootScope, $filter) {

}]);
